int[] array = { 2, 4, 3, 5, 7, 8, 9 };
for (int i = 0; i < array.Length; i++)
{
    for (int j = i + 1; j < array.Length; j++)
    {
        if (array[i] + array[j] == 10)
        {
            Console.WriteLine(array[i] + " + " + array[j] + " = 10");
        }
    }
}
